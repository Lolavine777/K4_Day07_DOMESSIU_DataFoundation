# Benchmark notes - Lương Minh Quân

The official common benchmark result is 8/10.

FixedSizeChunker with 500-character chunks and 50-character overlap answered queries 2 through 5 with grounded evidence.

Query 1 missed because fixed-size boundaries separated the care and fabric evidence.

The full run is recorded in `benchmark/team_results.json`.
