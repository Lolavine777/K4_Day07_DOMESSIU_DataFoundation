# Benchmark notes - Đào Minh Chiến

The official common benchmark result is 10/10.

RecursiveChunker with 500-character chunks retrieved and grounded all five golden queries when evaluated with BGE-M3.

The full run is recorded in `benchmark/team_results.json`.
