# Benchmark notes - Nguyễn Đăng Long

The official common benchmark result is 10/10.

HeadingRecursiveChunker with BGE-M3 retrieved every expected document at top-1 and the deduplicated agent context contained the complete evidence for all five answers.

The full run is recorded in `benchmark/team_results.json` and the personal run is recorded in `benchmark_results.json`.
